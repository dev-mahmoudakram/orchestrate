import { EmptyState } from "@/components/ui/empty-state";

export default function AdminPartnersPage() {
  return (
    <EmptyState
      description="Partner records, logo/image fields, sort order, and publish state will be added in CMS phases."
      title="Partners manager placeholder"
    />
  );
}
