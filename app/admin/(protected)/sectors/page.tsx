import { EmptyState } from "@/components/ui/empty-state";

export default function AdminSectorsPage() {
  return (
    <EmptyState
      description="Sector CRUD will provide filtering support for public project listings."
      title="Sectors manager placeholder"
    />
  );
}
