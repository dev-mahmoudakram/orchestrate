import { EmptyState } from "@/components/ui/empty-state";

export default function AdminSettingsPage() {
  return (
    <EmptyState
      description="Site settings for contact data, social links, and global metadata will be implemented with the database."
      title="Settings placeholder"
    />
  );
}
