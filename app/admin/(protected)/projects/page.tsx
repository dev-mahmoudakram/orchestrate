import { EmptyState } from "@/components/ui/empty-state";

export default function AdminProjectsPage() {
  return (
    <EmptyState
      description="Project CRUD, featured markers, sector relation, and result fields will be added later."
      title="Projects manager placeholder"
    />
  );
}
