import { EmptyState } from "@/components/ui/empty-state";

export default function AdminTranslationsPage() {
  return (
    <EmptyState
      description="Dynamic UI translation keys, grouping, search, and missing English indicators will be added after auth."
      title="Translation panel placeholder"
    />
  );
}
